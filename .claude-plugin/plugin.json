{
  "$comment": [
    "PLACEHOLDER NAME — the rename gate (founding design §12.4) is NOT satisfied. `claude-` is",
    "Anthropic's trademark and this id would be permanent the moment anyone installs it, so this",
    "manifest exists to make the plugin runnable LOCALLY and must not be published or added to a",
    "marketplace until the CEO renames the project."
  ],
  "name": "claude-plugin-spec",
  "version": "0.1.0",
  "description": "Builds a requirement source-of-truth out of your codebase and keeps it true after implementation: one truth, and every behaviour proven.",
  "author": { "name": "fumia" },
  "hooks": "./hooks/hooks.json",
  "skills": "./skills"
}
