---
slug: workspace-spec
title: Workspace
entrypoint: true
---

## Shape

The rates service is specified in [[api-spec]]. A retired note lives at [[missing-doc]] — the
target does not exist, so the fixture pins the `broken-link` issue.
